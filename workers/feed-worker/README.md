# feed-worker (Later)

Purpose
- Fan-out posts to a denormalized read model for faster feed reads at scale.

Status
- Deferred for MVP. API performs read-time composition; future worker will handle write-time fan-out.

